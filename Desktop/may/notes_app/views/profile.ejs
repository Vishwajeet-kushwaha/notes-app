<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full  min-h-screen text-zinc-500 bg-zinc-900 p-10">
        <div class="fixed top-0 right-0 flex justify-end">
            <a class="bg-red-400 rounded-md px-3 py-2 text-sm mb-5" href="/logout">Logout</a>
          </div>
           <h3 class="text-3xl mb-5 text-zinc-600 py-2"> Hello <%= user.name%></h3>
            <h5 class="mb-5">You can create a post.</h5>
            <form action="/post" method="post">
                <textarea class="w-1/3 resize-none bg-transparent block border-2 border-zinc-800 rounded-md"  name="content" placeholder="Write here"></textarea>
                <input class="px-3 w-43 text-sm py-2 bg-blue-400 mt-2 rounded-md block" type="submit" value="Create">
            </form>


            <div class="posts mt-20">
                <h3 class="text-zinc-500">Your posts:</h3>
                <% if (user.posts.length > 0) { %>
                  <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-5 rounded-md mb-4 border-3 bg-zinc-800 border-zinc-700">
                      <h4 class="text-blue-500"><%= user.username %></h4>
                      <p class="text-sm tracking-tight"><%= post.content %></p>
                      <small class="mt-1 inline-block"><%=post.likes.length %></small>
                      <div class="btns flex  gap-6">
                        <a class="text-blue-500" href="/like/<%= post._id%>">   <%= post.likes.indexOf(user.id)===-1 ? "Unlike":"Like" %></a>
                        <a class="text-green-500" href="/edit/<%= post._id%>">Edit</a>
                      </div>
                    </div>
                  <% }) %>
                <% } else { %>
                  <p>No posts yet!</p>
                <% } %>
              </div>
            
              
    </div>
</body>

</html>